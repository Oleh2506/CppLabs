#pragma once

#include "Quadrangle.h"

float getRandomFloatNumber();

void printQuadrangleArray(Quadrangle* quadranglesArray, const size_t arrSize);

float getMaxPerimeter(Quadrangle* quadranglesArray, const size_t arrSize);

void fillRandomQuadrangleArray(Quadrangle* quadranglesArray, size_t arrSize);
